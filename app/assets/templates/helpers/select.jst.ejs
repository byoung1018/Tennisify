<div class="<%= label %>">
    <label><%= toTitle(label) %>
      <select name="<%=model%>[<%=label%>]">
        <%itemNames.forEach(function(itemName){%>
            <option value="<%=itemName%>">
              <%=toTitle(itemName)%>
            </option>
        <%}) %>
      </select>
    </label>
</div>
