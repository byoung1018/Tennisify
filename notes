check line 20 of composite view
double check composite view

add hover to guest button

add invitees to meeting
  on click of invite toggle button
    save meeting as model
    render invite view and pass meeting model
  on click of save invitees
    creates invite collection from data
    render meeting and passes it the meeting model and invitee collection
    change meeting view to take in meeting model and invitees collection
    display invitees in meeting edit
  on click save meeting
    change save to create invitees

1a. create model for invitees
    create collection for invitees

2. add create invite button to meeting form
    on button click
      have button create a model for meeting
      render invite view and pass it the meeting

3. create invite view
    check modals and replace modal
    view:
      box for search
        should filter down users
      list of all users below
      add button
      list of users to add
      save button
      cancel button
    on save click
      create collection from added users
      render meeting view passing it the collection and meeting model
    on cancel
      render meeting view passing it the collection and meeting model
4. change meeting create to render invited users
    pass the collection into the template
    add box for invited users
5. change meeting save on controller to create the invite entries
    check on collection saving

fix editing to invite people

    have invite box

  add display invitees to show meeting with scroll
  have public checkboxes toggle the display


move navbar over to backbone


how to route to create meeting if popup is from create meeting and to index if not

add calculate age function
drop age column
remove from intake
create age function on model
put it into jbuilder



add about for users

not allow to accept if filters not met
level
age
gender

can't create blank meetings

transition for index to show



data-dismiss makes the current modal window force close

data-toggle opens up a new modal with the href content inside it

<a data-dismiss="modal" data-toggle="modal" href="#lost">Click</a>
or

<a data-dismiss="modal" onclick="call the new div here">Click</a>
do let us know if it works.












add invites personal invites to meetings
  add invited_users to db - meeting, user ids, invite
  add model assoc

  add create invites-

   ...

  add to json
  add

  add invitations for private meetings


filter by public/private current user
add emails
